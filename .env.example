## Replace variables below with the required settings ##
## DO NOT use quotes

## ## ## ## ## ## ## ## ## ##
# Hubzilla/Site config      #
#
#   NOTES:
# REGISTER_POLICY: options are REGISTER_OPEN, REGISTER_APPROVE, OR REGISTER_CLOSED
DOMAIN=localhost
ADMIN_EMAIL=admin@example.com
TIMEZONE=Etc/UTC
REQUIRE_EMAIL=1
REGISTER_POLICY=REGISTER_OPEN
ADDON_LIST=logrot nsfw superblock diaspora pubcrawl

## ## ## ## ## ## ## ## ## ##
# Database config           #
# 
#   NOTES:
# DB_HOST: needs to be the name/heading (not container name) of the database container (see docker-compose.yml)
# DB_TYPE Options: postgres or mariadb

## ## ## ## ##
# DB Agnostic
DB_HOST=hub_db
DB_NAME=hub
DB_USER=hubzilla
DB_PASSWORD=P@55w0rD

## ## ## ## ##
# Postgres
DB_TYPE=postgres
DB_PORT=5432

# PostgreSQL Docker container environment variables
POSTGRES_PASSWORD=P@55w0rD
POSTGRES_USER=hubzilla
POSTGRES_DB=hub

## ## ## ## ## ## ## ## ## ##
# SMTP Config               #
#
#   NOTES:
# Using local Stalwart mail server for development

# Local Stalwart SMTP Configuration
SMTP_HOST=stalwart
SMTP_PORT=587
SMTP_DOMAIN=example.com
SMTP_USER=admin@example.com
SMTP_PASS=admin123
SMTP_USE_STARTTLS=YES

# Stalwart Mail Server Configuration
STALWART_ADMIN_PASSWORD=admin123

# Python Email Processing Settings
IMAP_HOST=localhost
IMAP_PORT=143
IMAP_USE_SSL=false
IMAP_FOLDER=INBOX
IMAP_MARK_READ=false
DEBUG=true
LOG_LEVEL=INFO

# CalDAV Configuration
CALDAV_BASE_URL=https://localhost/cdav/

# Email to Channel Routing (format: email:channel|email:channel)
# Routes calendar invites to appropriate Hubzilla channels
EMAIL_CHANNEL_MAPPING=tech@example.com:tech|music@example.com:music|education@example.com:education|volunteer@example.com:volunteer|community@example.com:community|admin@example.com:admin

# Docker Service Names (must match service names in docker-compose.yml)
WEBSERVER_SERVICE_NAME=hub_web

## ## ## ## ## ## ## ## ## ##
# Logging                   #
#
#   NOTES:
# LOGROT_SIZE = size in bytes
ENABLE_LOGROT=0
LOGROT_PATH=log
LOGROT_SIZE=5242880
LOGROT_MAXFILES=20

## ## ## ## ## ## ## ## ## ##
# Debug                     #
DEBUG_PHP=0

# Define path for mkcert volume mount in docker-compose.yml
# MKCERT_PATH=~/.local/share/mkcert
MKCERT_PATH=/mnt/c/Users/mattb/AppData/Local/mkcert
